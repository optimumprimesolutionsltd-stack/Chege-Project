diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index aef944c..22d33d6 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -58,15 +58,19 @@ jobs:
       - name: Build what Render builds
         run: pnpm run build:render
 
-  staging-category-verification:
-    name: Verify staging category integrity
+  category-integrity-verification:
+    name: Verify category integrity
     # Do not expose the staging credential to code from forked repositories.
     if: >-
       github.event_name == 'pull_request' &&
       github.event.pull_request.head.repo.full_name == github.repository
     runs-on: ubuntu-latest
     env:
-      STAGING_DATABASE_URL: ${{ secrets.STAGING_DATABASE_URL }}
+      # This secret was named STAGING_DATABASE_URL and held a *production*
+      # connection string, so a failure here read as a staging problem for
+      # weeks while it was reporting on live data. Prefer the honestly named
+      # secret; fall back to the old one so nothing breaks before it is added.
+      CATEGORY_CHECK_DATABASE_URL: ${{ secrets.CATEGORY_CHECK_DATABASE_URL || secrets.STAGING_DATABASE_URL }}
     steps:
       - uses: actions/checkout@v4
       - uses: pnpm/action-setup@v4
@@ -75,12 +79,14 @@ jobs:
           node-version: 24
           cache: pnpm
       - run: pnpm install --frozen-lockfile --config.strictDepBuilds=false
-      - name: Require staging secret
+      - name: Require the database secret
         run: |
-          if [ -z "$STAGING_DATABASE_URL" ]; then
-            echo "::error::Repository secret STAGING_DATABASE_URL is not configured. Add it before relying on this PR check."
+          if [ -z "$CATEGORY_CHECK_DATABASE_URL" ]; then
+            echo "::error::Neither CATEGORY_CHECK_DATABASE_URL nor STAGING_DATABASE_URL is configured. Add one before relying on this PR check."
             exit 1
           fi
-          echo "Staging database secret is configured."
-      - name: Verify staging category integrity
-        run: pnpm --filter @workspace/scripts verify:staging-categories
+          echo "Database secret is configured."
+      - name: Verify category integrity
+        # Prints the database name it connected to. Read that line before
+        # acting on a failure — it is not necessarily staging.
+        run: pnpm --filter @workspace/scripts verify:category-integrity
