/**
 * The colour every group icon uses. A group owner can still pick an accent
 * colour and it is still saved, but it is never painted on what other
 * members see: the app is Jamvi's colour for everyone, whoever owns the group
 * they joined.
 */
export const DEFAULT_WORKSPACE_ACCENT = "#003383";
